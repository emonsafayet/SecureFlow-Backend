namespace SecureFlow.Shared.Authorization;

public static class Resources
{
    // Core
    public const string Dashboard = nameof(Dashboard);
    public const string Users = nameof(Users);
    public const string Roles = nameof(Roles);
    public const string Menus = nameof(Menus);

    // Business
    public const string Vendors = nameof(Vendors);
    public const string Evidences = nameof(Evidences);
    public const string Documents = nameof(Documents);

    // Settings
    public const string Settings = nameof(Settings);
    public const string AuditLogs = nameof(AuditLogs);
}
